pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://reposilite.iktdev.no/releases'
        }
        maven {
            url 'https://reposilite.iktdev.no/snapshots'
        }
    }
}
rootProject.name = "Demo Application - Widget"
include ':app'
include ':library:widget'